# Schema

Put protocol definitions here.

Recommended format:

- JSON Schema files for each message
- one envelope schema for message framing
- explicit versioning once clients outside your control exist
